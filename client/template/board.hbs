<thead>
  <tr>
    <th></th>
    {{#each columns}}
      <th>{{ this }}</th>
    {{/each}}
  </tr>
</thead>
<tbody>
  {{#each spaces}}
    <tr>
      {{#each .}}
        {{> template_space}}
      {{/each}}
    </tr>
  {{/each}}
</tbody>
