<thead>
  <tr>
    <th></th>
    {{#each columns}}
      <th>{{ this }}</th>
    {{/each}}
  </tr>
</thead>
<tbody>
  {{#each rows}}
    <tr>
      <td>{{ this }}</td>
      {{#each ../columns}}
        {{> template_boardCell}}
      {{/each}}
    </tr>
  {{/each}}
</tbody>
